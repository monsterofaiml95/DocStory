<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Download Documents</title>
  <style>
    /* Reset some default styles */
    body, h1, ul, li, a {
      margin: 0;
      padding: 0;
    }

    /* Apply a background color to the entire page */
    body {
      background-color: #f7f7f7; /* Light gray for a calming effect */
      font-family: Arial, sans-serif;
      text-align: center;
    }

    /* Style the header */
    h1 {
      background-color: #6062cb; /* A deep blue for the header background */
      color: #fff;
      padding: 20px 0;
    }

    /* Style the list of documents */
    ul {
      list-style: none;
      margin-top: 20px;
    }

    li {
      margin: 10px 0;
    }

    /* Style the document links */
    a {
      display: inline-block;
      background-color: #3267bc; /* A deep blue for trustworthiness */
      color: #fff;
      text-decoration: none;
      padding: 10px 20px;
      border-radius: 5px;
      transition: background-color 0.3s ease-in-out;
    }

    a:hover {
      background-color: #3267bc; /* Darker blue on hover */
    }

    a.back-link {
      margin: 0 10px;
            padding: 10px 25px;
            background-color: #3267bc; 
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: all 0.3s ease-in-out;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.128);
    }
    a.back-link:hover {
            background-color: #3267bc;
            transform: translateY(-2px);
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
        }
  </style>
</head>
<body>
  <h1>Download Documents</h1>
  <ul>
    <% documents.forEach(document => { %>
      <li>
        <a href="/download/<%= document._id %>"><%= document.name %></a>
      </li>
    <% }); %>
  </ul>
  <div class="back-link">
    <a href="/">back to upload</a>
  </div>
</body>
</html>
