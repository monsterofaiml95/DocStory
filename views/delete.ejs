
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Delete Documents</title>
  <style>
    /* Style for the popup */
    body,h1{
      margin: 0px;
      padding: 0px;
      background-color: white;
    }

    h1{
      font-family: Arial, sans-serif;
      padding: 25px 0;

      background-color: #6062cb;
      color: #fff;
      text-align: center;
    }
    .popup {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.5);
      z-index: 1;
      justify-content: center;
      align-items: center;
    }
    
    /* Style for the popup content */
    .popup-content {
      background-color: #fff;
      padding: 20px;
      border-radius: 5px;
      text-align: center;
    }
    ul {
  list-style: none;
  margin: 0; /* Add this line to remove default margin */
  padding: 0; /* Add this line to remove default padding */
}
    li{
      list-style: none;
      background-color: red;
      padding: 7px;
      border-radius: 5px;
      width: 90%;
      margin: auto;
      margin-top: 7px;
    }
    ul a{
      color: #fff;
      text-decoration: none;
    }
    .container{
      max-width: 500px;
      margin: auto;
    }
    .button-wrapper {
    display: flex;
    justify-content: center;
    margin-top: 20px;
}
a.back-link {
    display: inline-block; 
    margin: 0 10px;
    padding: 10px 25px;
    background-color: #3267bc; 
    color: white;
    text-decoration: none;
    border-radius: 5px;
    transition: all 0.3s ease-in-out;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.128);
}

a.back-link:hover {
    background-color: #3267bc;
    transform: translateY(-2px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
}

  </style>
</head>
<body>
  <h1>Click on the File to Delete it...</h1>
  <div class="container">
  <ul>
    <% documents.forEach(document => { %>
        <a href="#" onclick="openPopup('<%= document._id %>', '<%= document.name %>')"><li><%= document.name %></li></a>
    <% }); %>
  </ul>
</div>
<div class="button-wrapper">
  <a href="/" class="back-link">Back to Upload</a>
</div>


  <!-- Popup container -->
  <div class="popup" id="deletePopup">
    <div class="popup-content">
      <p>Are you sure you want to delete "<span id="documentName"></span>"?</p>
      <button onclick="confirmDelete()">Delete</button>
      <button onclick="closePopup()">Cancel</button>
    </div>
  </div>

  <script>
    function openPopup(documentId, documentName) {
      // Set the document name in the popup
      document.getElementById("documentName").textContent = documentName;
      this.documentId=documentId;
      
      // Display the popup
      document.getElementById("deletePopup").style.display = "block";
    }

    function closePopup() {
      // Close the popup
      document.getElementById("deletePopup").style.display = "none";
    }

    function confirmDelete() {
      // Redirect to the delete route
      window.location.href = `/delete/${this.documentId}`;
    }
  </script>
</body>
</html>
